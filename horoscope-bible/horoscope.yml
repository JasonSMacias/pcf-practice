---
platform: linux

inputs:
  - name: local-input

image_resource:
    type: docker-image
    # source: {repository: ubuntu}
    source: {repository: dwdraju/alpine-curl-jq}

run:
    path: ./local-input/horoscope-bible/horoscope.sh 
    args: [leo]